<div class="mb-3">
    <label for="{{$name}}" class="form-label">{{$label}} :</label>
    <input type="{{$type}}" id="{{$name}}" name="{{$name}}" class="form-control" placeholder="{{$placeholder}}">
</div>